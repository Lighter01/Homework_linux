#ifndef WRITE_FILE_H
#define WRITE_FILE_H

#include <fstream>
#include <sstream>
#include <filesystem>
#include <map>
#include "user.h"

void generateSyntheticDataset(std::map<std::string, user>& , const bool& );

#endif // WRITE_FILE_H
